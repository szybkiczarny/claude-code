# Sesja 2026-04-04

## Co robiliśmy
- Skonfigurowano backup repozytorium `szybkiczarny/claude-code` na GitHub
  - Token zapisany w git remote URL (`.git/config`)
  - Skrypt `backup.sh` + cron codziennie o 2:23
- Uruchomiono dashboard Device Inventor (`http://192.168.1.34:8765`)
  - Skrypt: `dashboard/server.py`
  - Autostart przez systemd user service (`~/.config/systemd/user/dashboard.service`)
  - Port 8765 odblokowany w UFW (`sudo ufw allow 8765/tcp`) — działa
- Generator pomysłów ustawiony na cron 3x dziennie (8:03, 14:03, 20:03)
  - Zmieniono liczbę pomysłów z 8 na 6 (3+3)

## Co warto pamiętać
- Git nie był zainicjowany lokalnie na początku sesji — `git init` było potrzebne
- UFW aktywny, tylko port 22 otwarty domyślnie — przy nowych serwisach pamiętaj o `ufw allow`
- Kuba woli sam wpisywać komendy gdy chodzi o sudo/hasła

## Stan na koniec
- Crontab: backup + generator pomysłów aktywne
- Dashboard: działa, autostart skonfigurowany
- GitHub: repo zsynchronizowane
