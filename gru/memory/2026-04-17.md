# Sesja 2026-04-17

## Co robiliśmy
- Wróciliśmy do projektu CS2DROP (`/home/kuba/CLAUDE_CODE/cs2-market`)
- Uruchomiliśmy serwer dev z `--hostname 0.0.0.0 --port 3000` (nasłuchuje na sieci)
- Kuba próbował otworzyć stronę z innej maszyny — dostał ERR_NETWORK_CHANGED (błąd przeglądarki przy zmianie sieci, nie serwera)
- Sesja urwana przed weryfikacją czy strona w końcu zadziałała

## Stan serwera
- Proces `nohup npm run dev -- --hostname 0.0.0.0 --port 3000` uruchomiony, PID ~26828
- Log: `/tmp/cs2-dev.log`
- Adresy: http://192.168.1.34:3000 (LAN), http://100.93.94.126:3000 (Tailscale)

## Co jeszcze do zrobienia w projekcie
- Brak `.env` → brak bazy PostgreSQL → app działa na mock data
- Prawdziwe obrazki skinów (Steam CDN)
- WebSockety dla live drops
- Battle/Upgrade logika

## Warto pamiętać
- Node 18 systemowy nie działa — zawsze używać nvm (Node 20+)
- Next.js dev domyślnie binduje tylko localhost — przy dostępie z LAN trzeba `--hostname 0.0.0.0`
