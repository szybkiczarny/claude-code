# Sesja 2026-04-21

## Co robiliśmy
Duża sesja rozbudowy construct-ai (React Native Expo).

## Zaimplementowane

### Storage
- Cloudflare R2 skonfigurowany (bucket: construct-ai-photos)
- Cloudflare Worker do uploadu zdjęć: `https://construct-ai-upload.construct-ai.workers.dev`
- Worker w `/home/kuba/CLAUDE_CODE/projects/r2-worker/`
- Zdjęcia usterek idą przez Worker → R2 → publiczny URL

### Funkcje
- **Zmiana statusu usterki** (open→in_progress→resolved) — w ProjectDetail i ReportDetail
- **Tabela contractors** — ekran kontrahentów z telefonem/emailem/WhatsApp
- **Tab Zadania** — next_steps z AI, checkbox, send via WhatsApp/SMS/Email
- **Tab Postęp** — progress_log z AI (% zaawansowania prac)
- **DefectCameraScreen** — podpięty do nawigacji we wszystkich stackach
- **AI prompt** — lepsze wykrywanie firm ("firma Nowacki ma ogarnąć")
- **NativeWind + lucide** — zainstalowane na przyszłość
- **Email przez Resend** — Supabase Edge Function `send-defect-email` gotowa

### Delete / Edit / Pull-to-refresh (dodane na końcu sesji)
- **ProjectsScreen** — długi przytisk lub ⋮ → Edytuj (modal: nazwa/adres/klient) / Usuń; pull-to-refresh; 4-kafelkowy dashboard (Projekty/Usterki/Raporty/Ten tydzień)
- **ReportsScreen** — długi przytisk → Usuń raport; pull-to-refresh
- **ReportDetailScreen** — przycisk 🗑 w nagłówku → Usuń raport; przycisk 🗑 przy każdej usterce; przycisk 🖼 (czerwony) przy usterce ze zdjęciem → Usuń zdjęcie; pull-to-refresh
- **ProjectDetailScreen** — pull-to-refresh we wszystkich 7 tabach; przycisk 🗑 przy każdej usterce; przycisk 🗑 przy każdym zadaniu

### Nowe tabele Supabase
- `contractors` (name, email, phone, trade, project_id)
- `tasks` (description, location, deadline, status, project_id, report_id)
- `progress_log` (percent, stage, note, project_id, report_id)
- `defect_history` (defect_id, old_status, new_status, changed_at) — tabela gotowa w SQL, UI nie wdrożone

### Bugi naprawione
- Linking nie był zaimportowany → WhatsApp/SMS nie działały
- DefectCamera tylko w ProjectsStack → błąd nawigacji z innych stacków
- TypeScript errors: progress w gemini.ts, EncodingType w r2.ts
- expo-constants brak → `npx expo install expo-constants`

## Stack
- Upload: Cloudflare Worker + R2 (zamiast Cloudinary/Supabase Storage)
- AI: Groq Whisper + LLaMA 3.3 70b
- DB: Supabase

## Do zrobienia (na jutro)
- Historia zmian statusu usterki — UI timeline (tabela `defect_history` istnieje)
- Push notifications dla zbliżających się terminów (expo-notifications zainstalowane, nie podpięte)
- PDF eksport raportu
- Testowanie kamera end-to-end
