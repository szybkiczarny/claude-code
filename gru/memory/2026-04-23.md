# Sesja 2026-04-23

## Co robiliśmy
- Dodaliśmy zakładkę **Statystyki** (4. tab w bottom nav) — usterki per status, per projekt, top wykonawcy
- Dodaliśmy zakładkę **Pogoda** w ProjectDetailScreen — geocoding adresu przez Nominatim + 7-dniowa prognoza Open-Meteo (oba darmowe, bez klucza API)
- Naprawiliśmy **kamerę do usterek** — FAB (złoty przycisk) w zakładkach Usterki/Do zrobienia otwiera DefectCameraScreen
- Poprawiliśmy **DefectCameraScreen** — dodano pola: opis usterki, lokalizacja, wybór powagi; stary kod miał hardcoded opis
- Dodano **PDF eksport raportu** — przycisk 📄 w headerze ReportDetailScreen, generuje HTML→PDF przez expo-print, udostępnia przez expo-sharing
- Poprawiono **AuthScreen** — reset hasła ("Nie pamiętam hasła"), potwierdzenie rejestracji emailem
- Skonfigurowano **EAS Build**: eas.json, package=com.constructai.app, versionCode=1
- Wgrano zmienne środowiskowe do EAS (`eas env:create`) — `.env` nie jest w gicie więc pierwszy build crashował
- Zbudowano APK (preview) — drugi build (z env vars) działa

## Co warto pamiętać
- `.env` nie jest w gicie → przy każdym nowym projekcie EAS trzeba wgrać zmienne przez `eas env:create`
- EAS account: michalwalk10, project ID: ce846357-a991-47f0-bbdd-75f1915a5023
- Pierwszy build crashował bo brakowało kluczy API — objaw: app wchodzi na 0.01s i wraca na pulpit
- Nominatim wymaga nagłówka `User-Agent` w fetch, inaczej może odrzucić zapytanie
- Open-Meteo: całkowicie darmowy, bez klucza, API: `api.open-meteo.com/v1/forecast`

## Co zostało
1. Podmienić ikonę — użytkownik ma zdjęcie na telefonie (plik: 20260423_135338_0000.png), chce je wgrać jako assets/icon.png
2. Play Store — konto Google Play Console + `eas build --profile production` (AAB)
3. Email do wykonawcy (Resend API) — pominięty celowo
4. Push notyfikacje
