# Sesja 2026-04-25

## Co robiliśmy
- Dodaliśmy **push notyfikacje** do ConstructAI

## Co zrobiliśmy

### Frontend
- `src/lib/notifications.ts` — nowy plik: rejestracja uprawnień, zapis tokenu do `push_tokens`, codzienny digest 8:00, deadline reminder dla delegowanych usterek
- `App.tsx` — `registerForPushNotifications()` + `scheduleDailyDigest()` wywoływane po zalogowaniu
- `ProjectDetailScreen.tsx` — po delegowaniu usterki z deadline → `scheduleDeadlineReminder()`
- `DefectCameraScreen.tsx` — po zapisaniu usterki → wywołuje Edge Function `send-push`
- `app.json` — dodany plugin `expo-notifications` (icon: icon.png)
- `tsconfig.json` — dodano `"exclude": ["supabase/functions"]` żeby wyeliminować błędy Deno

### Backend
- Tabela `push_tokens` (user_id PK → token, RLS: własny rekord) — **już utworzona w bazie przez API**
- Edge Function `send-push` — pobiera manager_id projektu → token z push_tokens → wysyła przez Expo Push API — **już wdrożona**

## Ważne szczegóły

- `inspectors` tabela NIE istnieje w bazie (schema z pliku nigdy nie była wgrana). Tabele są: contractors, crew, defect_history, defects, materials, progress_log, projects, reports, tasks
- `projects` ma kolumnę `manager_id uuid` — na niej opiera się remote push
- Supabase access token: `~/.supabase/access-token`
- Supabase project ref: `umrgtstdxbcguubxklgf`

## Co zostało do zrobienia
1. **APK w toku** — build odpalony, link: https://expo.dev/accounts/michalwalk10/projects/construct-ai/builds/d74215fd-1d77-4db8-b8ea-93cf2143c15c
2. **Ikona** — podmienić assets/icon.png (zdjęcie użytkownika, 1024x1024)
3. **Play Store** — konto Google Play Console + `eas build --profile production`
