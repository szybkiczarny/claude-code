# Sesja 2026-05-06

## Co robiliśmy
- ConstructAI — nowe funkcje + bugfixy

## Co zbudowaliśmy

### Refaktoryzacja
- ProjectDetailScreen.tsx odchudzony z 870 linii do ~130 linii
- Istniejące taby przeniesione do `src/screens/projectTabs/`:
  - `shared.ts` — wspólne style i helpery
  - `EmptyState.tsx`, `DiaryTab.tsx`, `DefectListTab.tsx`, `ProgressTab.tsx`
  - `TasksTab.tsx`, `CrewTab.tsx`, `MaterialsTab.tsx`, `WeatherTab.tsx`, `DocumentsTab.tsx`

### Nowe taby
- **FloorPlanTab.tsx** — upload rzutu piętra (obraz PNG/JPG), widok z pinezkami usterek
  - Tapnij rysunek → inline formularz: opis + ważność → zapisuje usterkę z pin_x/pin_y (znormalizowane 0..1)
  - Pinezki kolorowane wg severity (green/orange/red/dark red)
  - Tapnij pinezkę → sheet z detalami
- **ScheduleTab.tsx** — harmonogram zadań z CPM
  - Algorytm: Kahn topo sort → forward pass (ES/EF) → backward pass (LS/LF) → float → critical
  - Wizualizacja: pionowa lista + poziomy bar chart, ścieżka krytyczna czerwona
  - Dependency picker — modal z listą zadań, blokuje cykl (descendant detection)
  - Status: todo/in_progress/done (tapnij kropkę żeby zmienić)

### Bugfixy
- `expo-notifications` crash w Expo Go (SDK 53+):
  - `App.tsx` — `LogBox.ignoreLogs(['expo-notifications: ...'])` blokuje warning zanim crashuje LogBox
  - `notifications.ts` — statyczny import z lepszym guard: `Constants.executionEnvironment === 'storeClient'`

### Typy i migracje
- `FloorPlan`, `ScheduleTask` dodane do `types/index.ts`
- `Defect` rozszerzony o `floor_plan_id`, `pin_x`, `pin_y`
- Migracje wgrane przez `npx supabase@1.207.9 db push --linked -p "Zzzgredek.19"` — DZIAŁAJĄ

## Co warto pamiętać
- Supabase push migracji: `cd projects/construct-ai && SUPABASE_ACCESS_TOKEN=$(cat ~/.supabase/access-token) npx supabase@1.207.9 db push --linked -p "Zzzgredek.19"` (hasło zapisane w MEMORY)
- `FileSystem.EncodingType` nie eksportowane z expo-file-system → używamy `'base64' as any`
- Pinezki na rzucie: koordynaty 0..1 znormalizowane (nie piksele!)
- Supabase Management API bywa 503 — wtedy push przez CLI działa normalnie

## Backlog na następną sesję
1. **Edycja ekipy i materiałów** — edit button na każdym wpisie (modal z formularzem)
2. **Ręczne dodawanie usterek** — forma bez kamery, w każdej chwili z poziomu projektu
3. **Dodawanie zdjęć do usterek** — w każdej chwili, + po nagraniu raportu gdy AI wykryje usterki
4. Ikona aplikacji (własne zdjęcie Kuby, 1024x1024)
5. Play Store (AAB build, `eas build --profile production`)
